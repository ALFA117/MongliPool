# Rust
target/

# Node
node_modules/
.env
.env.local

# Circuit build artifacts (large files)
circuits/build/
circuits/node_modules/

# Frontend build
frontend/dist/

# Trusted setup artifacts — keep local, never commit
*.ptau
circuits/*.zkey

# NOTE: frontend/public/keys/ is NOT excluded.
# withdraw.wasm, withdraw_final.zkey, verification_key.json MUST be committed
# so Vercel can serve them to the browser for client-side ZK proof generation.

# OS
.DS_Store
Thumbs.db