{
  "name": "monglipool-circuits",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "circom withdraw.circom --r1cs --wasm --sym --output ./build",
    "setup": "node scripts/setup.js",
    "test": "node scripts/test-proof.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "circomlibjs": "^0.1.7"
  }
}
